/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remove the experimental: { instrumentationHook: true } line entirely!
  // Next.js 15+ detects the instrumentation.ts file automatically.
};

export default nextConfig;